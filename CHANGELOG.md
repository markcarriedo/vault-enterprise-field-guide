
## 2026-09-09
- **feat(changelog): Auto-regenerate via a post-commit hook** - Manual regeneration already drifted 6 commits behind once - not reliable enough. This hook amends CHANGELOG.md straight into every commit automatically; see decision log for how it avoids infinite recursion.
- **chore: Regenerate CHANGELOG.md, 6 commits behind** - Forgot to regenerate this alongside the last several commits (the exact drift risk flagged when this became a manual, non-CI step).
- **docs(planning): Mark VPC/KMS/license prerequisites done** - terraform/prerequisites applied cleanly (27 resources), state confirmed in S3 (no local tfstate). Also cleaned up 3 unrelated lab-bucket-* S3 buckets found in the sandbox account during an earlier audit - leftover AWS training-lab artifacts, not part of this project.
- **feat(terraform): Apply the state-bucket bootstrap, wire up S3 backend** - Bootstrap applied (6 resources) - state bucket vault-enterprise-field-guide-tfstate-dc2565bd now exists. Pointed terraform/prerequisites at it via backend "s3" with use_lockfile = true, confirmed with init + plan (still 27 to add, no drift, no local state file created).
- **feat(terraform): Scaffold state-bucket bootstrap and prerequisites config** - Two Terraform configs, neither applied yet (both plans reviewed): terraform/bootstrap creates an S3 bucket for remote state (versioned, KMS-encrypted, public access blocked, insecure-transport denied) using S3 native locking (use_lockfile) instead of a DynamoDB table - GA since Terraform 1.11, so no extra table to manage. terraform/prerequisites provisions the VPC (community terraform-aws-modules/vpc/aws module), the Vault auto-unseal KMS key, and the license Secrets Manager entry (read from the local .hclic file, never committed).  Neither provider block takes a `profile` argument - credentials come from plain AWS SDK environment variables per session, nothing persisted to ~/.aws/credentials. See decision log for why.
- **fix(mkdocs): Enable pymdownx.tilde so ~~strikethrough~~ actually renders** - Same gap as the tasklist checkbox fix - the extension was never enabled, so completed-step markers in the planning page rendered as literal tildes instead of struck-through text.
- **docs(planning): Record the full deployment sequence, local-only Terraform** - Confirmed with the user: Terraform runs locally, no CI/CD for the AWS infra build. That settles cloud-credential and state-backend questions that would otherwise need answering (OIDC role vs local profile, remote vs local state) - state stays local and gitignored since it can hold sensitive values and there's no pipeline to need a shared backend.
- **fix(mkdocs): Render task-list checkboxes instead of literal [ ]** - pymdownx.tasklist was never enabled, so "- [ ]" in the prerequisites checklist rendered as literal bracket text instead of an actual checkbox.
- **feat(changelog): Group entries by day instead of [Unreleased]** - This repo has no version tags and never will (continuously-updated docs site, not a release cadence), so "[Unreleased]" was permanently true and uninformative. Group entries under their author date instead - one `## YYYY-MM-DD` heading per day, newest day and newest commit first to match `git log` order. Each entry is a tight "**type(scope): message** - body" one-liner (multi-line bodies collapsed to spaces, no per-type subheadings or blank lines between entries), with type derived from raw_message rather than the emoji-labeled parser group.
- **docs: Move CHANGELOG.md to the repo root, out of the MkDocs site** - Previous setup embedded it into the site via a snippet-include, but a generated file that's also gitignored means nobody actually sees it except whoever last ran git-cliff locally. Root-level and committed is the actual convention (GitHub surfaces it, git clone gets it) and matches where people expect to find one. CI no longer regenerates it since the site build doesn't depend on it; it's now a manual step alongside each commit, same as running gitleaks locally already is.
- **fix(ci): Stop passing --config twice to git-cliff-action** - The action already builds --config=<config> from its own config input and appends args verbatim, so args also containing --config duplicated the flag and made git-cliff refuse to run. Verified the composed command locally before pushing this time.
- **docs: Replace journal with a git-cliff generated changelog** - Journal was hand-written narrative; decided reasoning can live in commit bodies instead, rendered into the changelog by a custom cliff.toml template (renders commit.body, not just the one-line summary). Adopting Conventional Commits from here on so git-cliff can group entries by type instead of dumping everything under "Other". Existing history is kept as-is in that "Other" bucket (filter_unconventional = false) rather than rewritten, since this repo is already public.  CHANGELOG.md is generated (git-cliff), gitignored, and rebuilt in CI before every Pages deploy; docs/changelog.md pulls it in via a pymdownx.snippets include.
- **style(nav): Drop the right-align CSS, keep Journal in normal tab flow**
- **style(nav): Reorder to Home/Guide/Reference/Journal, right-align Journal** - Guide is the actual deliverable, so it leads; Journal is supplementary raw notes, so it's visually separated on the right rather than mixed into the primary nav group.
- **docs(journal): Revive the journal, backfilled with the day's work** - Wiki was a dead end (its git repo can't be initialized without first creating a page through the web UI) so the journal lives back in docs/journal/, in the MkDocs nav as originally intended.
- **docs(planning): Record target environment and AWS survey findings** - HashiCorp sandbox account, ap-southeast-2. No account IDs/ARNs recorded since this repo is public. Survey confirms all four prerequisites need to be built from scratch (default VPC only, no usable KMS keys).
- **docs(planning): Document prerequisites for the HVD Vault Enterprise module** - Planning page now tracks the four prerequisites (VPC, KMS key, license + TLS material in Secrets Manager); decision log records why we're using HashiCorp's official HVD module instead of hand-rolled Terraform.
- **style(theme): Use Vault's official brand colors and logo** - Colors and icon sourced from hashicorp/design-system (vault-brand #ffcf25, vault-500 #9a6f00 for accessible light-mode links, vault-200 #ffe543 for dark-mode links). Black header/footer chrome matches the "brand-alt" token HashiCorp itself uses where the gold doesn't work as chrome.
- **ci(pages): Deploy docs to GitHub Pages via Actions** - Wires up the Pages URL in mkdocs.yml, the repo homepage field, and the README.
- **docs(journal): Drop the journal, guide + reference are the published output** - Journaling stays a working process, not a published artifact.
- **docs(readme): Lead with the Vault content, not MkDocs mechanics**
- **chore(security): Add gitleaks pre-commit hook** - Blocks commits containing credentials, keys, or tokens before they land in git history.
- **chore(mkdocs): Scaffold field guide site with Material theme** - Material theme, journal/guide/reference structure, mermaid support.
